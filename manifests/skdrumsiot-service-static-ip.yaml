apiVersion: v1
kind: Service
metadata:
  name: skdrumsiot
  labels:
    app: iot
spec:
  selector:
    app: iot
    tier: api
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
  loadBalancerIP: "YOUR.IP.ADDRESS.HERE"
